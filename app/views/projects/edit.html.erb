<h2> Edit Project </h2>

<%= form_for @project do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :start_date %>
  <%= f.date_field :start_date %>

  <%= f.submit :save %>
<% end %>

<%= link_to 'Back', projects_path %>